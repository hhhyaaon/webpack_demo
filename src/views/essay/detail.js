import {Component} from "react"
import ReactDOM from "react-dom"

export class Datail extends Component {
    constructor(props) {
        super(props);
    }
    render() {
        return (
            <h1>Datail</h1>
        )
    }
}